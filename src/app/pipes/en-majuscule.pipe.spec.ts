import { EnMajusculePipe } from './en-majuscule.pipe';

describe('EnMajusculePipe', () => {
  it('create an instance', () => {
    const pipe = new EnMajusculePipe();
    expect(pipe).toBeTruthy();
  });
});
